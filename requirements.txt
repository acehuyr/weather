# ---- Core ----
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.6.0

# ---- Data analysis ----
pandas>=2.1.0
numpy>=1.26.0

# ---- LLM (free tier: console.groq.com, no card needed) ----
groq>=0.11.0

# ---- API + UI ----
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
streamlit>=1.32.0
plotly>=5.19.0

# ---- Map (both free, no API key) ----
folium>=0.16.0
streamlit-folium>=0.20.0

# ---- Testing ----
pytest>=8.0.0

# ---- Optional: better RAG quality (see README "RAG configuration") ----
# sentence-transformers>=2.6.0
# chromadb>=0.4.24

# ---- Optional: paid alternative to Groq ----
# anthropic>=0.40.0
